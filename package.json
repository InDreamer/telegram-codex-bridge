{
  "name": "telegram-codex-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ctb": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/codex/app-server.test.ts src/install.test.ts src/logger.test.ts src/service.test.ts src/state/store.test.ts src/telegram/poller.test.ts"
  },
  "engines": {
    "node": ">=25.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
