interface:
  display_name: "Telegram Codex Linker"
  short_description: "Precheck first, then run the bundled install path or ctb repair path with minimal user interruption."
  default_prompt: "Use $telegram-codex-linker to precheck my environment, then directly run the bundled bridge install script or the ctb repair path. Only interrupt me for unavoidable external steps like giving a bot token, logging into Codex, or messaging the bot once."
